<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>To Do List</title>
  <link rel="shortcut icon" href="favicon.ico">
  <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.8.1/css/all.css" integrity="sha384-50oBUHEmvpQ+1lW4y57PTFmhCaXp0ML5d60M1M7uH2+nqUivzIebhndOJK28anvf" crossorigin="anonymous">
  <link rel="stylesheet" href="styles.css">
</head>
<body>

  <section>
    <h1>To Do List</h1>
    <h2>Why you listing so hard?</h2>

    <ul class="messages">
    <% for(var i=0; i<messages.length; i++) {%>

      <% if( messages[i].checkMark === true){ %>
        <li class="message">
          <span class="done"><%= messages[i].name %></span>
          <span><i class="fas fa-check-square" aria-hidden="true"></i></span>
          <span><i class="fa fa-trash" aria-hidden="true"></i></span>
        </li>
       <% } else{ %>
         <li class="message">
           <span class="notdone"><%= messages[i].name %></span>
           <span><i class="fas fa-check-square" aria-hidden="true"></i></span>
           <span><i class="fa fa-trash" aria-hidden="true"></i></span>
         </li>
      <% } %>
    <% } %>
    </ul>

    <h2>Add a list item</h2>

    <form action="/messages" method="POST">
      <input type="text" placeholder="text" name="name">
      <button type="submit">Submit</button>
    </form>
    <h3>Click check mark when a list item is completed</h3>
  </section>


  <script src="main.js"></script>

</body>
</html>
